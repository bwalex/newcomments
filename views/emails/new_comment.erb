Hi!

There is a new comment on the following article:

<%= article.name %> - <%= article.url %>

<%= comment.name %> (Email: <%= comment.email %>, IP: <%= comment.ip %>) said:

<%= comment.comment %>
